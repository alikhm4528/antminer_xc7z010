# AXI DMA Example
This example enables axi_dma and uses a provided sample generator ip in order to simulate the generation of streaming data. you can change the signal by changing the signal.mem file and repack the ip.
